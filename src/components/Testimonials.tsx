import { useState } from 'react'

function Testimonials() {
    const [count, setCount] = useState(0);

    return (
        <>
            <h2>testimonials section</h2>
            <p>
            Rotating reviews pulled from (TBD)
            </p>
        </>
    );
}

export default Testimonials;